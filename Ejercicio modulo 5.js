//Ejercicio modulo 5
let altura_centimetros = 180;
let altura_metros = 1.78;
let peso = 75.6;
let alt_redondeada = Math.round(altura_centimetros);
let peso_redondeado = Math.floor(peso);
let maximo = "El maximo valor que se puede obtener en Javascript + 1 = " + Number.MAX_VALUE;

